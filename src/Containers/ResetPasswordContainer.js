import React from 'react';
import ResetPasswordPresentation from '../Presentations/ResetPasswordPresentation';

export default class ResetPasswordContainer extends React.Component {
  render() {
    return (
      <ResetPasswordPresentation />
    );
  }
}
